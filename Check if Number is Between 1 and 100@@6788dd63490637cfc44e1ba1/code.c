#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    if(Enumerable.Range(1,100).Contains(n));
    {
    printf("In Range");
    }
    else
    {
        printf("Out of Range");
    }
    return 0;
}